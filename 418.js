exports.__esModule = true;

exports.default = function (t, c, f) {
  if (!module419.default(t)) module420.default(f + '() in ' + c + ' must return a plain object. Instead received ' + t + '.');
};

var module419 = require('./419'),
  module420 = require('./420');
